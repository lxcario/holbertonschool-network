#!/usr/bin/env bash
# Configures local DNS resolution for localhost and facebook.com

# Replace 127.0.0.1 with 127.0.0.2 in the hosts file
sed -i 's/127.0.0.1/127.0.0.2/g' /etc/hosts

# Append facebook.com resolution to the hosts file
echo "8.8.8.8 facebook.com" >> /etc/hosts